diff --git a/bin/omarchy-launch-shell b/bin/omarchy-launch-shell
index c183eb0b..195b6018 100755
--- a/bin/omarchy-launch-shell
+++ b/bin/omarchy-launch-shell
@@ -41 +41,5 @@ compositor_alive() {
-    hyprctl -j monitors >/dev/null 2>&1 && return 0
+    if [[ -n ${NIRI_SOCKET:-} ]]; then
+      niri msg version >/dev/null 2>&1 && return 0
+    else
+      hyprctl -j monitors >/dev/null 2>&1 && return 0
+    fi
diff --git a/shell/Ui/PopupCard.qml b/shell/Ui/PopupCard.qml
index 058b068f..fecf3c92 100644
--- a/shell/Ui/PopupCard.qml
+++ b/shell/Ui/PopupCard.qml
@@ -83 +83 @@ PopupWindow {
-    active: root.open && root.triggerMode === "click"
+    active: Quickshell.env("NIRI_SOCKET") === "" && root.open && root.triggerMode === "click"
diff --git a/shell/Commons/Style.qml b/shell/Commons/Style.qml
index ec61252b..b8617a11 100644
--- a/shell/Commons/Style.qml
+++ b/shell/Commons/Style.qml
@@ -350,0 +351 @@ QtObject {
+    if (Quickshell.env("NIRI_SOCKET") !== "") return
diff --git a/shell/plugins/bar/Bar.qml b/shell/plugins/bar/Bar.qml
index 5dcd205f..e85f1922 100644
--- a/shell/plugins/bar/Bar.qml
+++ b/shell/plugins/bar/Bar.qml
@@ -2,0 +3 @@ import Quickshell.Hyprland
+import Quickshell.Niri
@@ -490,0 +492,7 @@ Item {
+    if (Quickshell.env("NIRI_SOCKET") !== "") {
+      var workspaces = Niri.workspaces.values
+      for (var i = 0; i < workspaces.length; i++) {
+        if (workspaces[i].focused) return String(workspaces[i].output || "")
+      }
+      return ""
+    }
diff --git a/shell/plugins/bar/widgets/KeyboardLayout.qml b/shell/plugins/bar/widgets/KeyboardLayout.qml
index 5457d88e..edee9ae5 100644
--- a/shell/plugins/bar/widgets/KeyboardLayout.qml
+++ b/shell/plugins/bar/widgets/KeyboardLayout.qml
@@ -3,0 +4 @@ import Quickshell.Hyprland
+import Quickshell.Niri
@@ -12,0 +14 @@ BarWidget {
+  readonly property bool isNiri: Quickshell.env("NIRI_SOCKET") !== ""
@@ -40,0 +43,5 @@ BarWidget {
+    if (root.isNiri) {
+      root.layoutFull = String(Niri.currentKeyboardLayoutName || "")
+      root.multipleLayouts = Niri.keyboardLayoutNames.length > 1
+      return
+    }
@@ -74,0 +82,4 @@ BarWidget {
+    if (root.isNiri) {
+      Niri.dispatch(["switch-layout", "next"])
+      return
+    }
@@ -103,0 +115,6 @@ BarWidget {
+  Connections {
+    target: root.isNiri ? Niri : null
+    function onKeyboardLayoutsChanged() { root.refresh() }
+    function onKeyboardLayoutSwitched() { root.refresh() }
+  }
+
@@ -199 +216 @@ BarWidget {
-    running: !root.keyboardName || root.keyboardUnresolved || root.keyboardCount > 1
+    running: !root.isNiri && (!root.keyboardName || root.keyboardUnresolved || root.keyboardCount > 1)
diff --git a/shell/plugins/bar/widgets/Workspaces.qml b/shell/plugins/bar/widgets/Workspaces.qml
index ec0482a5..0d37d0cc 100644
--- a/shell/plugins/bar/widgets/Workspaces.qml
+++ b/shell/plugins/bar/widgets/Workspaces.qml
@@ -2,0 +3 @@ import QtQuick.Layouts
+import Quickshell
@@ -3,0 +5 @@ import Quickshell.Hyprland
+import Quickshell.Niri
@@ -11,4 +13,16 @@ BarWidget {
-  function workspaceById(id) {
-    var values = Hyprland.workspaces.values
-    for (var i = 0; i < values.length; i++) {
-      if (values[i].id === id) return values[i]
+  readonly property bool isNiri: Quickshell.env("NIRI_SOCKET") !== ""
+  readonly property string screenName: root.QsWindow.window && root.QsWindow.window.screen
+    ? String(root.QsWindow.window.screen.name || "") : ""
+  property int workspaceRevision: 0
+
+  function workspaceEntries() {
+    var revision = workspaceRevision
+    if (isNiri) {
+      var niriEntries = []
+      var niriWorkspaces = Niri.workspaces.values
+      for (var n = 0; n < niriWorkspaces.length; n++) {
+        var workspace = niriWorkspaces[n]
+        if (root.screenName && String(workspace.output || "") !== root.screenName) continue
+        niriEntries.push({ workspace: workspace, id: workspace.id, label: String(workspace.idx) })
+      }
+      return niriEntries
@@ -17,4 +30,0 @@ BarWidget {
-    return null
-  }
-
-  function workspaceIds() {
@@ -30 +40,10 @@ BarWidget {
-    return ids
+    return ids.map(function(id) {
+      var workspace = null
+      for (var i = 0; i < values.length; i++) {
+        if (values[i].id === id) {
+          workspace = values[i]
+          break
+        }
+      }
+      return { workspace: workspace, id: id, label: id === 10 ? "0" : String(id) }
+    })
@@ -33 +52 @@ BarWidget {
-  function focusWorkspace(id) {
+  function focusWorkspace(entry) {
@@ -35 +54,17 @@ BarWidget {
-    root.bar.run("hyprctl dispatch " + Util.shellQuote("hl.dsp.focus({ workspace = \"" + id + "\" })"))
+    if (root.isNiri) {
+      if (entry.workspace && entry.workspace.output)
+        Niri.dispatch(["focus-monitor", String(entry.workspace.output)])
+      Niri.dispatch(["focus-workspace", String(entry.workspace.idx)])
+      return
+    }
+    root.bar.run("hyprctl dispatch " + Util.shellQuote("hl.dsp.focus({ workspace = \"" + entry.id + "\" })"))
+  }
+
+  Connections {
+    target: root.isNiri ? Niri : null
+    function onWorkspacesUpdated() { root.workspaceRevision++ }
+  }
+
+  Connections {
+    target: root.isNiri ? null : Hyprland
+    function onRawEvent(event) { root.workspaceRevision++ }
@@ -47 +82 @@ BarWidget {
-    columns: root.vertical ? 1 : root.workspaceIds().length
+    columns: root.vertical ? 1 : root.workspaceEntries().length
@@ -52 +87 @@ BarWidget {
-      model: root.workspaceIds()
+      model: root.workspaceEntries()
@@ -55 +90 @@ BarWidget {
-        required property int modelData
+        required property var modelData
@@ -57,3 +92,7 @@ BarWidget {
-        readonly property var workspace: root.workspaceById(modelData)
-        readonly property bool occupied: workspace !== null && workspace.toplevels.values.length > 0
-        readonly property bool focused: Hyprland.focusedWorkspace !== null && Hyprland.focusedWorkspace.id === modelData
+        readonly property var workspace: modelData.workspace
+        readonly property bool occupied: root.isNiri
+          ? workspace !== null && workspace.occupied
+          : workspace !== null && workspace.toplevels.values.length > 0
+        readonly property bool focused: root.isNiri
+          ? workspace !== null && workspace.focused
+          : Hyprland.focusedWorkspace !== null && Hyprland.focusedWorkspace.id === modelData.id
@@ -62 +101 @@ BarWidget {
-        text: focused ? "\uDB85\uDCFB" : (modelData === 10 ? "0" : String(modelData))
+        text: focused ? "\uDB85\uDCFB" : modelData.label
diff --git a/shell/plugins/notifications/Service.qml b/shell/plugins/notifications/Service.qml
index 0122310e..77ea56f4 100644
--- a/shell/plugins/notifications/Service.qml
+++ b/shell/plugins/notifications/Service.qml
@@ -399,2 +399,2 @@ Item {
-  // Try to focus an existing Hyprland window matching the notification's
-  // sender. The helper handles case-insensitive class matching.
+  // Try to focus an existing compositor window matching the notification's
+  // sender. The helper handles case-insensitive application matching.
@@ -404 +404 @@ Item {
-      service.omarchyPath + "/bin/omarchy-hyprland-focus-app",
+      service.omarchyPath + "/bin/omarchy-focus-app",
diff --git a/shell/plugins/services/idle/Service.qml b/shell/plugins/services/idle/Service.qml
index a57b3921..8df42bc0 100644
--- a/shell/plugins/services/idle/Service.qml
+++ b/shell/plugins/services/idle/Service.qml
@@ -18,0 +19 @@ Item {
+  readonly property bool screensaverEnabled: idleConfig.screensaver !== false
@@ -21 +22 @@ Item {
-  readonly property int firstIdleTimeoutSeconds: Math.min(screensaverTimeoutSeconds, lockTimeoutSeconds)
+  readonly property int firstIdleTimeoutSeconds: screensaverEnabled ? Math.min(screensaverTimeoutSeconds, lockTimeoutSeconds) : lockTimeoutSeconds
@@ -91 +92 @@ Item {
-    if (root.screensaverDelaySeconds === 0) launchScreensaver()
+    if (root.screensaverEnabled && root.screensaverDelaySeconds === 0) launchScreensaver()
@@ -92 +93 @@ Item {
-    else screensaverTimer.restart()
+    else if (root.screensaverEnabled) screensaverTimer.restart()
@@ -310 +311 @@ Item {
-    target: Hyprland
+    target: Quickshell.env("NIRI_SOCKET") === "" ? Hyprland : null
diff --git a/shell/plugins/lock/Service.qml b/shell/plugins/lock/Service.qml
index c144f0d2..33225d2e 100644
--- a/shell/plugins/lock/Service.qml
+++ b/shell/plugins/lock/Service.qml
@@ -391 +391 @@ Item {
-    command: ["bash", "-c", "omarchy-hyprland-session-locked"]
+    command: ["bash", "-c", "if [[ -n $NIRI_SOCKET ]]; then exit 1; else omarchy-hyprland-session-locked; fi"]
@@ -411 +411 @@ Item {
-    command: ["bash", "-c", "omarchy-brightness-keyboard off; omarchy-brightness-display off"]
+    command: ["bash", "-c", "if [[ -n $NIRI_SOCKET ]]; then niri msg action power-off-monitors; else omarchy-brightness-keyboard off; omarchy-brightness-display off; fi"]
